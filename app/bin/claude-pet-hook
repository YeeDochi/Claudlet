#!/usr/bin/env python3
# DEPRECATED backward-compat path (pre-rename settings.json). Remove next minor;
# claudlet-install-hooks migrates old entries to bin/claudlet-hook.
import _shim; _shim.run("claudlet.cli.hook:main", swallow=True)
