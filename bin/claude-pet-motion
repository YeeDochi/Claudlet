#!/usr/bin/env python3
"""Thin shim -> claude_pet.motion:main."""
import os, sys
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "src"))
try:
    from claude_pet.motion import main
    sys.exit(main(sys.argv))
except Exception:
    sys.exit(0)
