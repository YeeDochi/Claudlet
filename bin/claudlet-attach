#!/usr/bin/env python3
"""Thin shim -> claudlet.attach:main (source-checkout attach helper)."""
import os, sys
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "src"))
try:
    from claudlet.attach import main
    sys.exit(main(sys.argv[1:]))
except Exception:
    sys.exit(0)
