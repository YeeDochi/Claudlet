#!/usr/bin/env python3
"""Thin shim -> claude_pet.hook (never fails Claude; always exits 0)."""
import os, sys
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "src"))
try:
    from claude_pet.hook import main
    main()
except Exception:
    pass
sys.exit(0)
